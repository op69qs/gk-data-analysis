# Agent Key File Handler 全量迁移设计

## 1. 目标与范围

将 `agentKeyFileHandler-2.1.0.jar` 的全部业务能力复写并迁移到本仓库主系统：

- 后端承载模块：`org-tribe-system`；
- 前端承载模块：`org-tribe-view`；
- 数据库：Vastbase G100 V2.2；Vastbase 的 Schema 与原 MySQL Schema 一一对应；
- 迁移原则：保持原业务结果、数据口径和自动处理语义；修正旧 JAR 已发现的失效接口、目录遍历、同步阻塞、SQL 拼接与状态不可追踪问题；
- 不迁移原 JAR 的 EasyUI 页面、MySQL 专有 SQL、硬编码文件路径和明文配置。

本设计覆盖 KEY 与 TIMS 两个来源域，以及相关配置、监控、变更记录和加工调用。

| 来源域 | 文件与数据类型 | JAR 中业务能力 |
|---|---|---|
| KEY | 一个 ZIP 中的收入、支出、库存、退库文本文件 | 上传、解压、识别文件、解析、入库、待处理、监控、代理国库配置 |
| TIMS | 收入、支出、库存 ZIP；内部为 Excel | 上传、解压、解析、入库、监控、自动处理、手动再次加工 |
| 公共 | 国库范围、变更记录、日志、状态展示 | 查询、异常展示、删除、重试、下游加工调用 |

## 2. 已确认决策

1. 采用“并入 `org-tribe-system` 的独立数据上报模块”，不新建微服务。
2. 保留 JAR 全部功能；不机械复制缺陷实现。
3. 原始 ZIP 与解压后的文件都必须保留，支持追溯、下载和重新处理。
4. 解析成功后自动执行下游加工；页面必须显示整个执行过程与最终结果。
5. 再次调用必须按本次上报批次记录的账期、数据类型和国库范围执行。
6. 当前使用菜单权限；设计中预留上报人员、审核/运维人员及全部按钮权限，后续启用时不改业务主体。
7. 所有确定与不确定事项均写入本文档和实施脚本；未确认的数据库对象不得猜测表结构或自行创建。

## 3. 架构选择

### 3.1 备选方案

| 方案 | 结论 | 原因 |
|---|---|---|
| 并入主系统的独立业务模块 | 采用 | 可复用主系统已有登录、菜单、审计、文件、任务、Vastbase 和 Vue 2 页面能力；当前页面规模与处理量无需独立部署 |
| 仓库内新增独立服务 | 不采用 | 需重复建设认证、权限、文件和调度，增加部署和运维成本 |
| 保持独立微服务 | 暂不采用 | 适合极大文件量或多系统复用场景；当前会增加网关、跨服务状态和授权复杂度 |

### 3.2 模块边界

后端在 `org.jeecg.modules` 下建立独立的 `reporting` 业务包，至少分为：批次与文件、KEY 解析、TIMS 解析、数据写入、下游加工、任务调度、过程日志和权限常量。前端在 `org-tribe-view` 下建立“数据上报”菜单域。

模块不直接依赖页面参数决定账期、目标表或过程名称；后台仅以已固化的批次、文件和任务记录执行。

## 4. 端到端业务流程

```text
上传 ZIP
  -> 原件归档并计算 SHA-256
  -> 建立上报批次和文件记录
  -> 安全解压并登记解压文件
  -> 后台解析 KEY/TIMS 文件
  -> 校验格式和业务字段，记录行级异常
  -> 写入对应明细/暂存对象
  -> 自动调用本批次账期的下游加工
  -> 记录过程调用结果
  -> 更新批次、文件、任务和时间线状态
```

页面关闭不会取消后台任务。上传、解析、入库和加工均为可追踪的后台任务；任何阶段失败都停止后续依赖阶段，但保留已完成结果、文件和异常信息，供授权人员重新执行。

### 4.1 批次状态

| 层级 | 状态 |
|---|---|
| 文件接收 | 待上传、上传中、已归档、上传失败 |
| 文件准备 | 待解压、解压中、解压成功、解压失败 |
| 数据解析 | 待解析、解析中、解析成功、解析异常 |
| 数据入库 | 待入库、入库中、入库成功、入库失败 |
| 下游加工 | 待调用、调用中、调用成功、调用失败 |
| 整体批次 | 处理中、成功、部分成功、失败、已取消、已逻辑删除 |

整体状态由任务状态汇总，不由前端直接改写。每个状态变迁写入任务日志，记录开始时间、结束时间、耗时、当前文件、文件/行数统计、错误摘要及关联操作人。

### 4.2 自动加工与重试

- 上传完成后自动提交解析任务；解析并入库成功后自动提交下游加工任务。
- 下游调用参数从批次读取，不允许前端随意传入其他期间。
- 原 JAR 调用过程的规则：把 `yyyy-MM` 转为当月最后一天，再调用 `adm.P_GUOKU_LIB_REPORT_ALL(账期)`。
- 重试可按失败阶段发起：重新解析、重新入库、重新调用加工；每次均新建任务执行记录并关联原批次。
- 同一账期/范围的下游加工必须互斥，防止并发重复处理。原 JAR 通过 `etl.guoku_lib_report_all_log` 中 `STATE='1'` 判断；新实现保留业务含义，并增加应用级锁与超时恢复。

## 5. 文件归档与解析规范

### 5.1 文件归档

文件二进制不存入数据库。服务端生成目录，不接受前端路径：

```text
<归档根目录>/reporting/<source-domain>/<accounting-period>/<batch-id>/
  original/       原始 ZIP
  extracted/      解压文件
  manifest.json   文件清单、摘要、处理结果快照
```

数据库保存原始名、归档名、相对路径、大小、SHA-256、父子文件关系、留存状态与逻辑删除状态。物理清理由独立保留策略任务执行，不由普通删除操作直接删除。

### 5.2 解压安全与兼容性

- 拒绝路径穿越、损坏 ZIP、超过配置数量/大小限制和不允许的文件类型；
- 递归扫描所有子目录；只把允许的 `.txt`、`.xls`、`.xlsx` 交给对应解析器；
- 忽略 `__MACOSX`、`.DS_Store`、`._*` 等 macOS 附属文件；
- 同时兼容 ZIP 根目录直接放业务文件、以及业务文件位于收入/支出/库存等子目录；
- 文件类型、压缩包声明类型、内部文件内容不一致时，记录明确异常。

已核对的脱敏样例 `收入.zip` 为“ZIP -> 收入目录 -> 收入1.xls、收入2.xls”的结构，并含 macOS 元数据目录。原 JAR 递归函数依赖目录遍历顺序，可能在多级目录或 macOS 附属目录下漏读；新实现必须采用完整递归收集，不能保留该缺陷。

### 5.3 待样例验证项

其他 KEY、TIMS 收入/支出/库存/退库样例后续补充。样例验证不会改变模块架构，仅可能补充解析适配规则。需核验：目录层级、文件编码、工作表、表头行、日期格式、字段列顺序、金额精度、空值和异常行表现。

## 6. 页面与权限规范

### 6.1 页面

1. **上报列表**：批次号、来源域、类型、账期、国库范围、文件、发起人、当前阶段、整体状态、进度、耗时和操作入口。
2. **上报详情**：按时间线展示上传、解压、解析、入库、加工；展示每步文件数、行数、首条错误、调用参数、开始/结束时间和结果。
3. **文件与异常明细**：原件/解压文件下载、文件解析结果、行级错误。
4. **KEY 代理国库配置**：保留原启停、有效期和查询能力。
5. **监控与变更记录**：保留原 KEY/TIMS 监控和收入/支出变更记录能力；查询逻辑以已确认的 EDW 对象为准。

前端异步轮询批次详情；任务完成、失败或进度变化均刷新状态。后续可在不改变任务模型的前提下替换为 WebSocket 推送。

### 6.2 权限

当前菜单权限为最小上线控制；服务端仍保留以下权限编码和校验点：

| 权限编码类别 | 预留操作 | 后续角色建议 |
|---|---|---|
| 菜单 | 上报、批次详情、监控、代理国库配置、变更记录 | 上报人员、审核/运维人员、管理员 |
| 文件 | 上传、查看、下载、逻辑删除、物理清理 | 上报人员；审核/运维人员；管理员 |
| 任务 | 重新解析、重新入库、重新加工、取消 | 审核/运维人员 |
| 配置 | 代理国库配置、保留策略 | 管理员 |
| 审计 | 查看完整异常、调用参数、任务日志 | 审核/运维人员、管理员 |

初期可将所有操作授权给同一菜单角色；后续启用按钮权限时，只增加菜单/按钮数据与角色授权，不改控制器、任务或页面业务逻辑。

## 7. JAR 对象基线与 Vastbase 核对项

### 7.1 JAR 明确引用的对象

| Schema | 对象 | 类型 | 操作 | 业务含义 | Vastbase 状态 |
|---|---|---|---|---|---|
| `agent_key_file` | `agent_treatury_config` | 表 | 查/新增/修改 | 代理国库配置 | 待核对 |
| `agent_key_file` | `agent_keyfile_pending` | 表 | 查/新增/修改/删除 | KEY 待处理及异常 | 待核对 |
| `agent_key_file` | `agent_file_income` | 表 | 删/写 | KEY 收入明细 | 待核对 |
| `agent_key_file` | `agent_file_payout` | 表 | 删/写 | KEY 支出明细 | 待核对 |
| `agent_key_file` | `agent_file_stock` | 表 | 删/写 | KEY 库存明细 | 待核对 |
| `agent_key_file` | `agent_file_back` | 表 | 删/写 | KEY 退库明细 | 待核对 |
| `agent_key_file` | `tims_file_pending` | 表 | 查/新增/修改/删除 | TIMS 待处理及异常 | 待核对 |
| `agent_key_file` | `tims_file_income` | 表 | 删/写 | TIMS 收入明细 | 待核对 |
| `agent_key_file` | `tims_file_payout` | 表 | 删/写 | TIMS 支出明细 | 待核对 |
| `agent_key_file` | `tims_file_stock` | 表 | 删/写 | TIMS 库存明细 | 待核对 |
| `stg` | `trs_tmis_budget_income` | 表 | 删/写 | TIMS 收入自动处理目标 | 待核对 |
| `stg` | `trs_tmis_budget_payout` | 表 | 删/写 | TIMS 支出自动处理目标 | 待核对 |
| `stg` | `trs_tmis_stock` | 表 | 删/写 | TIMS 库存自动处理目标 | 待核对 |
| `edw` | `cm_guoku_dimnsn` | 表或视图 | 查 | 国库范围/树 | 待核对 |
| `edw` | `income_report_detail_stat` | 表或视图 | 查 | 收入变更查询基线 | 待核对 |
| `edw` | `payout_report_detail_stat` | 表或视图 | 查 | 支出变更查询基线 | 待核对 |
| `edw` | `reprot_update_record` | 表 | 查/写 | 人工变更记录；原名为 `reprot` | 待核对 |
| `etl` | `guoku_lib_report_all_log` | 表 | 查/写 | 下游加工运行日志 | 待核对 |
| `adm` | `P_GUOKU_LIB_REPORT_ALL` | 存储过程 | 调用 | 按账期加工 | 待核对 |

JAR 的 `DATABASE=stg` 配置与反编译逻辑已唯一确定 TIMS 自动任务目标为三张 `stg.trs_tmis_*` 表；不存在 JAR 中库名不明的业务表。`dmcode`、`comm_sys`、`dps` 未被该 JAR 的 Mapper 直接引用，不列为 JAR 强制依赖；如主系统菜单、用户或字典接入依赖它们，应在实施中作为主系统集成依赖单列。

### 7.2 JAR 未提供、必须跟踪确认的事项

| 待确认项 | 为什么不能猜测 | 需要材料 |
|---|---|---|
| 上表在 Vastbase 是否存在 | 当前仅知 JAR 名称 | 对象清单或 DDL |
| 表字段、类型、默认值、主键、唯一约束、索引 | JAR Mapper 不能完整证明 DDL | `CREATE TABLE` 或导出 DDL |
| `adm.P_GUOKU_LIB_REPORT_ALL` 定义及全部依赖 | JAR 仅知道过程名和单个账期参数 | 过程 DDL、依赖对象、返回/异常约定 |
| `etl.guoku_lib_report_all_log` 列顺序和约束 | 原 JAR 插入未写列名 | 表 DDL |
| EDW 对象是表还是视图及可访问权限 | JAR 仅查询 | DDL/对象类型与授权信息 |
| 国库树与当前用户国库范围接口 | JAR 前端调用外部接口，JAR 内未实现 | 主系统现有接口、数据来源或替代规则 |
| 文件保留期限、容量和备份目录 | 已确认必须留存，具体保留规则尚未给定 | 运维/业务保留要求 |

## 8. Vastbase 脚本交付边界

脚本均在文件头写明目标 Schema、对象、执行前提、是否 DDL、是否允许重复执行和回滚方式。脚本分为：

```text
01_schema_object_inventory.sql
02_agent_key_file_structure_check.sql
03_stg_structure_check.sql
04_edw_etl_adm_dependency_check.sql
05_report_tracking_tables.sql
06_report_indexes_constraints.sql
07_mysql_vastbase_sql_compatibility_check.sql
08_data_reconciliation_check.sql
09_menu_permission_seed.sql
10_process_dependency_check.sql
11_rollback.sql
```

在既有 JAR 表 DDL 未确认前，脚本只生成核对报告，不覆盖或修改现有业务对象。新建的上报跟踪表归属以最终确认的应用落点为准；本文不假设其名称或 Schema。

转换规则包括：MySQL 反引号、`IFNULL`、`DATE_FORMAT`、`LIMIT offset,size`、`GROUP_CONCAT`、`FIND_IN_SET` 等语法改为 Vastbase 兼容 SQL；所有 `${}` 动态拼接改为白名单对象路由和参数绑定。

## 9. 验收与回滚

### 9.1 新旧对照验收

- 以脱敏样例和后续补充样例分别验证 KEY 与 TIMS 全部类型；
- 核对文件数、解析成功/失败行数、账期、国库、金额、目标表行数和加工结果；
- 验证根目录与多级目录、macOS 附属文件、重复上传、错误文件、加工失败和重试；
- 验证菜单可见性、未来按钮权限切换点和全链路操作日志；
- 由数据核对 SQL 对比迁移前后关键行数、金额汇总和异常数。

### 9.2 上线与回滚

- 上线前先以只读核对脚本确认对象与过程依赖；
- 先灰度执行一批脱敏/可回放文件，核对通过后开启自动加工；
- 失败时停止新任务投递，不删除原件和批次记录；
- 数据写入使用批次和任务边界控制，回滚脚本仅处理新模块创建的对象及可识别的新批次数据，不直接清理既有数仓对象。

## 10. 明确不迁移的旧实现缺陷

- TIMS 页面调用但 JAR 控制器不存在的 `/exeTimsFile` 接口；
- 目录递归仅返回最后一层文件数组、可能遗漏 Excel 的实现；
- 前端同步阻塞上传/调用；
- SQL 中 `${}` 直接拼接和 MySQL 专有函数；
- 仅以“已处理/未处理”表达过程状态，无法展示完整进度；
- 硬编码文件路径、账号口令、国库代码及外部服务地址。

上述内容不改变原始业务口径；新模块以批次、文件、任务和过程日志实现等价且可审计的业务结果。
