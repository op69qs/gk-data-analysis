# 动态刷数状态颜色调整设计

## 范围

- 动态刷数主列表。
- 运行记录弹窗。

## 设计

- 状态 `200`（成功）使用 Ant Design Vue 预设色 `green`。
- 状态 `500`（失败）使用 Ant Design Vue 预设色 `red`。
- 正在执行、未执行及未知状态保持现状。
- 不增加 CSS，不抽取公共方法，不修改状态文字或业务逻辑。

## 验证

按用户要求，本次仅调整颜色，不执行编译、测试或新增测试用例。
