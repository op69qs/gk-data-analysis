# 指标查询身份与数据范围实现计划

1. 用单元测试锁定基础 SQL 不含用户权限、门户/本地范围 SQL 及非法上下文失败关闭。
2. OAuth 回调缓存 `subject_code`，网关为指标服务注入可信用户 ID 与国库上下文。
3. 指标服务运行时包装查询 SQL；覆盖列表、图表、导出、账期、维度和已保存方案执行。
4. 方案、收藏和个人指标接口用服务端当前用户覆盖创建人/修改人，并收紧按所有者修改、删除。
5. 新增历史保存 SQL 去用户范围迁移与回滚脚本，随后执行模块测试、构建和测试库验证。
