<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.reporting.mapper.ReportWorkflowMapper">
    <select id="findLatestTask" resultType="org.jeecg.modules.reporting.entity.ReportTask">
        select *
          from agent_key_file.report_task
         where batch_id = #{batchId}
           and task_type = #{taskType}
         order by attempt_no desc, create_time desc
         limit 1
    </select>

    <select id="findBatchFiles" resultType="org.jeecg.modules.reporting.entity.ReportFile">
        select *
          from agent_key_file.report_file
         where batch_id = #{batchId}
           and del_flag = 0
         order by create_time, original_name
    </select>

    <select id="countExternalRunningProcess" resultType="int">
        select count(1)
          from etl.guoku_lib_report_all_log
         where state = '1'
    </select>

    <select id="countInternalRunningProcess" resultType="int">
        select count(1)
          from agent_key_file.report_process_call
         where accounting_period = #{accountingPeriod}
           and coalesce(treasury_scope, '') = coalesce(#{treasuryScope}, '')
           and status in ('QUEUED', 'PROCESSING')
    </select>

    <!-- 列顺序保持原 JAR；真实 ETL 表字段仍须以内网核验脚本确认。 -->
    <insert id="insertExternalProcessLog">
        insert into etl.guoku_lib_report_all_log
        values (#{id}, #{processName}, #{accountingPeriod}, '1', #{userId}, #{addTime}, null)
    </insert>

    <select id="callReportProcedure" statementType="CALLABLE">
        call adm.p_guoku_lib_report_all(#{accountingPeriod})
    </select>
</mapper>
