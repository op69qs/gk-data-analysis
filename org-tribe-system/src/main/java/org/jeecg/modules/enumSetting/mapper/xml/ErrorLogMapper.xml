<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.enumSetting.mapper.ErrorLogMapper">

    <sql id="taskColumns">
        id, task_name, shell_path, shell_name, shell_param, cron_id, status,
        create_time, create_user, update_time, update_user, task_type
    </sql>

    <select id="getData" resultType="java.util.HashMap">
        SELECT <include refid="taskColumns"/>
        FROM adm.exec_shell_task
        <where>
            <if test="params.task_name != null and params.task_name != ''">
                task_name LIKE CONCAT('%', #{params.task_name}, '%')
            </if>
            <if test="params.status != null and params.status != ''">
                AND status = #{params.status}
            </if>
        </where>
        ORDER BY create_time DESC, id DESC
        LIMIT #{params.rows} OFFSET #{params.page}
    </select>

    <select id="getCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM adm.exec_shell_task
        <where>
            <if test="params.task_name != null and params.task_name != ''">
                task_name LIKE CONCAT('%', #{params.task_name}, '%')
            </if>
            <if test="params.status != null and params.status != ''">
                AND status = #{params.status}
            </if>
        </where>
    </select>

    <select id="getTaskById" resultType="java.util.HashMap">
        SELECT <include refid="taskColumns"/>
        FROM adm.exec_shell_task
        WHERE id = #{id}
    </select>

    <insert id="add">
        INSERT INTO adm.exec_shell_task (
            id, task_name, shell_path, shell_name, shell_param, cron_id, status,
            create_time, create_user, update_time, update_user, task_type
        ) VALUES (
            #{params.id}, #{params.task_name}, #{params.shell_path}, #{params.shell_name},
            #{params.shell_param}, #{params.cron_id}, #{params.status}, #{params.create_time},
            #{params.create_user}, #{params.update_time}, #{params.update_user}, #{params.task_type}
        )
    </insert>

    <update id="edit">
        UPDATE adm.exec_shell_task
        SET task_name = #{params.task_name},
            shell_path = #{params.shell_path},
            shell_name = #{params.shell_name},
            shell_param = #{params.shell_param},
            cron_id = #{params.cron_id},
            task_type = #{params.task_type},
            status = '0',
            update_time = #{params.update_time},
            update_user = #{params.update_user}
        WHERE id = #{params.id}
          AND COALESCE(status, '0') != '1'
    </update>

    <delete id="del">
        DELETE FROM adm.exec_shell_task
        WHERE id = #{id}
          AND COALESCE(status, '0') != '1'
    </delete>

    <update id="markTaskRunning">
        UPDATE adm.exec_shell_task
        SET status = '1', update_time = TO_CHAR(CURRENT_TIMESTAMP, 'YYYY-MM-DD HH24:MI:SS')
        WHERE id = #{id}
          AND COALESCE(status, '0') != '1'
    </update>

    <update id="updateTaskStatus">
        UPDATE adm.exec_shell_task
        SET status = #{status}, update_time = TO_CHAR(CURRENT_TIMESTAMP, 'YYYY-MM-DD HH24:MI:SS')
        WHERE id = #{id}
    </update>

</mapper>
