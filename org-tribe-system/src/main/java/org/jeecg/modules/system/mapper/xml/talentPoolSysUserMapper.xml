<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.system.mapper.TalentPoolSysUserMapper">
    <!-- author: Samer 人才库与用户表关联-->

    <!-- 获取用户对应的人才库姓名 -->
    <select id="getRelationTalentPoolName" resultType="java.util.HashMap">
        SELECT
            a.*,
            c.UUID,
            c.NAME
        FROM
            "jeecg-boot-os".sys_user a
            LEFT JOIN
            inspection.inspection_sysuser_talentpool_relation b
                ON a.id = b.SYS_USERID
            LEFT JOIN
            inspection.cm_guoku_talentpool c
                ON b.TALENTPOOL_ID = c.UUID
        WHERE
            a.id = #{params.id}
    </select>

    <!-- 修改关联关系 -->
    <insert id="editTalentPoolSysUserRelation">
        UPDATE inspection.inspection_sysuser_talentpool_relation
        SET
            TALENTPOOL_ID = #{params.TALENTPOOL_ID}
        WHERE
            SYS_USERID = #{params.SYS_USERID}
    </insert>

    <!-- 是否已存在关系 -->
    <select id="isExistRelation" resultType="java.util.HashMap">
        SELECT
        *
        FROM
        inspection.inspection_sysuser_talentpool_relation a
        WHERE
        <if test=' params.SYS_USERID != null and params.SYS_USERID != "" '>
            a.SYS_USERID = #{params.SYS_USERID}
            AND (a.TALENTPOOL_ID IS NOT NULL OR a.TALENTPOOL_ID &lt;&gt; '')
        </if>
        <if test=' params.TALENTPOOL_ID != null and params.TALENTPOOL_ID != "" '>
            a.TALENTPOOL_ID = #{params.TALENTPOOL_ID}
            AND (a.SYS_USERID IS NOT NULL OR a.SYS_USERID &lt;&gt; '')
        </if>
    </select>

    <!-- 删除已有关系 -->
    <delete id="delExistRelation">
        DELETE FROM
        inspection.inspection_sysuser_talentpool_relation
        WHERE (
        SYS_USERID = #{params.SYS_USERID}
        OR
        TALENTPOOL_ID = #{params.TALENTPOOL_ID}
        )
    </delete>

    <!-- 新增关联关系 -->
    <insert id="addTalentPoolSysUserRelation">
        INSERT INTO inspection.inspection_sysuser_talentpool_relation
        VALUES (
            #{params.SYS_USERID},
            #{params.TALENTPOOL_ID},
            '0'
        )
    </insert>


</mapper>